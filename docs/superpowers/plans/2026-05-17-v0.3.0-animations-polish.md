# v0.3.0 Animations & Polish — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the current "values appear instantly" and "progress bar leaps 0 → 50 → 100" feel with a coherent, confident animation vocabulary across the dashboard and installer.

**Architecture:** Two small reusable controls (`AnimatedCounter`, `RunCleanerButton`) in the dashboard plus one (`PhaseList`) in the bootstrapper. Shared theme tokens for all durations and easings. Two pure-C# state classes in `GraceKeeper.Core` (`PhaseProgressTracker`, `CleanerCompletionWatcher`) cover the non-visual logic — both unit-tested. Visual behavior is manual-QA'd via the release checklist (we have no WPF UI test harness and adding one is out of scope).

**Tech Stack:** WPF on .NET Framework 4.8 (`GraceKeeper.UI`, `GraceKeeper.Bootstrapper`), .NET Standard 2.0 (`GraceKeeper.Core`), xUnit (`GraceKeeper.Core.Tests` on net8.0).

**Spec:** `docs/superpowers/specs/2026-05-17-v0.3.0-animations-polish-design.md`.

---

## File Map

**New files:**
- `src/GraceKeeper.UI/Controls/AnimatedCounter.cs` + `AnimatedCounter.xaml` — odometer-style number tween.
- `src/GraceKeeper.UI/Controls/RunCleanerButton.cs` + `RunCleanerButton.xaml` — three-state morphing button (Idle/Running/Done).
- `src/GraceKeeper.Bootstrapper/Controls/PhaseList.xaml` + `PhaseList.xaml.cs` + `PhaseRow.cs` — installer phase rows with VSM-driven dot/spinner/check.
- `src/GraceKeeper.Core/PhaseProgressTracker.cs` — pure state machine for phase rows.
- `src/GraceKeeper.Core.Tests/PhaseProgressTrackerTests.cs`
- `src/GraceKeeper.Core/CleanerCompletionWatcher.cs` — polls cleaner-log offsets for a new Clean entry up to a timeout.
- `src/GraceKeeper.Core.Tests/CleanerCompletionWatcherTests.cs`

**Existing files modified:**
- `src/GraceKeeper.UI/Themes/Dark.xaml` — add Duration + Easing tokens.
- `src/GraceKeeper.UI/Themes/Light.xaml` — same.
- `src/GraceKeeper.Bootstrapper/Themes/Theme.xaml` — same.
- `src/GraceKeeper.UI/Views/DashboardView.xaml` — wire AnimatedCounter, log slide-in, status pulse, RunCleanerButton.
- `src/GraceKeeper.UI/Views/DashboardView.xaml.cs` — adjust click handler if needed (binding to State).
- `src/GraceKeeper.UI/ViewModels/DashboardViewModel.cs` — add `CleanerState`, integrate `CleanerCompletionWatcher`.
- `src/GraceKeeper.Bootstrapper/Views/ProgressPage.xaml` — bind to `SmoothedPercent`, add shimmer overlay, mount `PhaseList`.
- `src/GraceKeeper.Bootstrapper/Views/WelcomePage.xaml` — hero word cascade.
- `src/GraceKeeper.Bootstrapper/MainWindow.xaml` — `ContentControl` page transition wrapper.
- `src/GraceKeeper.Bootstrapper/ViewModels/MainViewModel.cs` — `SmoothedPercent`, `IsRunning`, `Phases`, lifecycle wiring through `PhaseProgressTracker`.
- `docs/release-checklist.md` — new QA items for the animation pass.

---

## Build / Test Invocations (reference)

- Build all: `dotnet build src/GraceKeeper.sln -c Release`
- Run tests: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj -c Release --no-build`
- Run a single test: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj --filter "FullyQualifiedName~PhaseProgressTrackerTests"`

---

## Task 1: Shared theme tokens

**Files:**
- Modify: `src/GraceKeeper.UI/Themes/Dark.xaml`
- Modify: `src/GraceKeeper.UI/Themes/Light.xaml`
- Modify: `src/GraceKeeper.Bootstrapper/Themes/Theme.xaml`

- [ ] **Step 1: Add tokens to Dark.xaml**

Edit `src/GraceKeeper.UI/Themes/Dark.xaml`. Add inside `<ResourceDictionary>` (before the closing tag), keeping existing brushes intact and adding the `System` xmlns at the root:

Change the root tag to add the system namespace:
```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:sys="clr-namespace:System;assembly=mscorlib">
```

Then add inside the dictionary, right before `</ResourceDictionary>`:
```xml
    <!-- Animation tokens -->
    <Duration x:Key="Duration.Quick">0:0:0.12</Duration>
    <Duration x:Key="Duration.Standard">0:0:0.24</Duration>
    <Duration x:Key="Duration.Slow">0:0:0.48</Duration>
    <Duration x:Key="Duration.Counter">0:0:1.2</Duration>
    <Duration x:Key="Duration.Pulse">0:0:1.6</Duration>

    <CubicEase x:Key="Easing.Standard" EasingMode="EaseOut"/>
    <QuinticEase x:Key="Easing.Decelerate" EasingMode="EaseOut"/>
```

- [ ] **Step 2: Add the same tokens to Light.xaml**

Same change to `src/GraceKeeper.UI/Themes/Light.xaml` — add the `xmlns:sys` to the root and add the same Duration + Ease blocks before `</ResourceDictionary>`.

- [ ] **Step 3: Add the same tokens to bootstrapper Theme.xaml**

Same change to `src/GraceKeeper.Bootstrapper/Themes/Theme.xaml`. The root already has `xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"` and `xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"`; add the same Duration + Ease blocks before `</ResourceDictionary>`.

- [ ] **Step 4: Build to verify XAML parses**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds with no XAML errors.

- [ ] **Step 5: Commit**

```
git add src/GraceKeeper.UI/Themes/Dark.xaml src/GraceKeeper.UI/Themes/Light.xaml src/GraceKeeper.Bootstrapper/Themes/Theme.xaml
git commit -m "ui(themes): add shared Duration and Easing tokens"
```

---

## Task 2: AnimatedCounter control

**Files:**
- Create: `src/GraceKeeper.UI/Controls/AnimatedCounter.cs`
- Create: `src/GraceKeeper.UI/Controls/AnimatedCounter.xaml` (Generic.xaml-style theme dictionary or merged dict)

- [ ] **Step 1: Create the control class**

Create `src/GraceKeeper.UI/Controls/AnimatedCounter.cs`:

```csharp
using System;
using System.Globalization;
using System.Windows;
using System.Windows.Controls;
using System.Windows.Media.Animation;

namespace GraceKeeper.UI.Controls;

public class AnimatedCounter : Control
{
    static AnimatedCounter()
    {
        DefaultStyleKeyProperty.OverrideMetadata(
            typeof(AnimatedCounter),
            new FrameworkPropertyMetadata(typeof(AnimatedCounter)));
    }

    public static readonly DependencyProperty ValueProperty =
        DependencyProperty.Register(
            nameof(Value), typeof(long), typeof(AnimatedCounter),
            new PropertyMetadata(0L, OnValueChanged));

    public static readonly DependencyProperty DisplayValueProperty =
        DependencyProperty.Register(
            nameof(DisplayValue), typeof(double), typeof(AnimatedCounter),
            new PropertyMetadata(0.0, OnDisplayValueChanged));

    public static readonly DependencyProperty FormattedTextProperty =
        DependencyProperty.Register(
            nameof(FormattedText), typeof(string), typeof(AnimatedCounter),
            new PropertyMetadata("0"));

    public static readonly DependencyProperty RollDurationProperty =
        DependencyProperty.Register(
            nameof(RollDuration), typeof(Duration), typeof(AnimatedCounter),
            new PropertyMetadata(new Duration(TimeSpan.FromMilliseconds(1200))));

    public static readonly DependencyProperty EasingProperty =
        DependencyProperty.Register(
            nameof(Easing), typeof(IEasingFunction), typeof(AnimatedCounter),
            new PropertyMetadata(new QuinticEase { EasingMode = EasingMode.EaseOut }));

    public long Value
    {
        get => (long)GetValue(ValueProperty);
        set => SetValue(ValueProperty, value);
    }

    public double DisplayValue
    {
        get => (double)GetValue(DisplayValueProperty);
        private set => SetValue(DisplayValueProperty, value);
    }

    public string FormattedText
    {
        get => (string)GetValue(FormattedTextProperty);
        private set => SetValue(FormattedTextProperty, value);
    }

    public Duration RollDuration
    {
        get => (Duration)GetValue(RollDurationProperty);
        set => SetValue(RollDurationProperty, value);
    }

    public IEasingFunction Easing
    {
        get => (IEasingFunction)GetValue(EasingProperty);
        set => SetValue(EasingProperty, value);
    }

    private static void OnValueChanged(DependencyObject d, DependencyPropertyChangedEventArgs e)
    {
        var c = (AnimatedCounter)d;
        var target = (long)e.NewValue;
        var anim = new DoubleAnimation
        {
            To = target,
            Duration = c.RollDuration,
            EasingFunction = c.Easing,
            FillBehavior = FillBehavior.HoldEnd,
        };
        c.BeginAnimation(DisplayValueProperty, anim, HandoffBehavior.SnapshotAndReplace);
    }

    private static void OnDisplayValueChanged(DependencyObject d, DependencyPropertyChangedEventArgs e)
    {
        var c = (AnimatedCounter)d;
        var v = (long)(double)e.NewValue;
        c.FormattedText = v.ToString("N0", CultureInfo.CurrentCulture);
    }
}
```

- [ ] **Step 2: Create the default template**

Create `src/GraceKeeper.UI/Controls/AnimatedCounter.xaml`:

```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctl="clr-namespace:GraceKeeper.UI.Controls">
    <Style TargetType="{x:Type ctl:AnimatedCounter}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctl:AnimatedCounter}">
                    <TextBlock Text="{TemplateBinding FormattedText}"
                               FontSize="{TemplateBinding FontSize}"
                               FontWeight="{TemplateBinding FontWeight}"
                               FontFamily="{TemplateBinding FontFamily}"
                               Foreground="{TemplateBinding Foreground}"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
```

- [ ] **Step 3: Merge the control template into App.xaml**

Edit `src/GraceKeeper.UI/App.xaml`. Add the AnimatedCounter resource dictionary to the merged dictionaries list:

```xml
<ResourceDictionary.MergedDictionaries>
    <ResourceDictionary Source="Themes/Light.xaml" />
    <ResourceDictionary Source="Controls/AnimatedCounter.xaml" />
</ResourceDictionary.MergedDictionaries>
```

- [ ] **Step 4: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds. The XAML build action for `AnimatedCounter.xaml` should be `Page` by default since it's in the project tree; if Build complains the file isn't part of the project, add it as `<Page>` in the .csproj or rely on SDK glob inclusion (the SDK includes `**/*.xaml` automatically for UseWPF).

- [ ] **Step 5: Commit**

```
git add src/GraceKeeper.UI/Controls/AnimatedCounter.cs src/GraceKeeper.UI/Controls/AnimatedCounter.xaml src/GraceKeeper.UI/App.xaml
git commit -m "ui: add AnimatedCounter control for tween-style number rolls"
```

---

## Task 3: Wire AnimatedCounter into DashboardView

**Files:**
- Modify: `src/GraceKeeper.UI/Views/DashboardView.xaml`

- [ ] **Step 1: Add the controls namespace**

In `src/GraceKeeper.UI/Views/DashboardView.xaml`, the root `<Page ...>` element already has `xmlns:vm` and `xmlns:conv`. Add `xmlns:ctl="clr-namespace:GraceKeeper.UI.Controls"` alongside them.

- [ ] **Step 2: Replace the three large counter TextBlocks**

Find this block (lines ~66–72):
```xml
<StackPanel>
    <TextBlock Text="{Binding PopupsDismissedLifetime}" FontSize="28" FontWeight="SemiBold"/>
    <TextBlock Text="Popups dismissed" FontSize="11" Foreground="{DynamicResource Text.Muted}"/>
    <TextBlock Text="{Binding PopupsToday, StringFormat='+{0} today'}" FontSize="11"
               Foreground="{DynamicResource Semantic.Good}" Margin="0,5,0,0"/>
</StackPanel>
```

Replace with:
```xml
<StackPanel>
    <ctl:AnimatedCounter Value="{Binding PopupsDismissedLifetime}" FontSize="28" FontWeight="SemiBold"/>
    <TextBlock Text="Popups dismissed" FontSize="11" Foreground="{DynamicResource Text.Muted}"/>
    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
        <TextBlock Text="+" FontSize="11" Foreground="{DynamicResource Semantic.Good}"/>
        <ctl:AnimatedCounter Value="{Binding PopupsToday}" FontSize="11"
                             Foreground="{DynamicResource Semantic.Good}"
                             RollDuration="0:0:0.6"/>
        <TextBlock Text=" today" FontSize="11" Foreground="{DynamicResource Semantic.Good}"/>
    </StackPanel>
</StackPanel>
```

- [ ] **Step 3: Same swap for the `.rnl files deleted` card**

Find:
```xml
<StackPanel>
    <TextBlock Text="{Binding RnlFilesDeletedLifetime}" FontSize="28" FontWeight="SemiBold"/>
    <TextBlock Text=".rnl files deleted" FontSize="11" Foreground="{DynamicResource Text.Muted}"/>
    <TextBlock Text="{Binding FilesToday, StringFormat='+{0} today'}" FontSize="11"
               Foreground="{DynamicResource Semantic.Good}" Margin="0,5,0,0"/>
</StackPanel>
```

Replace with:
```xml
<StackPanel>
    <ctl:AnimatedCounter Value="{Binding RnlFilesDeletedLifetime}" FontSize="28" FontWeight="SemiBold"/>
    <TextBlock Text=".rnl files deleted" FontSize="11" Foreground="{DynamicResource Text.Muted}"/>
    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
        <TextBlock Text="+" FontSize="11" Foreground="{DynamicResource Semantic.Good}"/>
        <ctl:AnimatedCounter Value="{Binding FilesToday}" FontSize="11"
                             Foreground="{DynamicResource Semantic.Good}"
                             RollDuration="0:0:0.6"/>
        <TextBlock Text=" today" FontSize="11" Foreground="{DynamicResource Semantic.Good}"/>
    </StackPanel>
</StackPanel>
```

Note: leave the third card (`TimeSinceLastCleanText`) alone — it's a string, not a counter.

- [ ] **Step 4: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 5: Commit**

```
git add src/GraceKeeper.UI/Views/DashboardView.xaml
git commit -m "ui(dashboard): use AnimatedCounter for hero metric cards"
```

---

## Task 4: Activity log slide-in storyboard

**Files:**
- Modify: `src/GraceKeeper.UI/Views/DashboardView.xaml`

- [ ] **Step 1: Add ItemContainerStyle to the activity ItemsControl**

Find the `<ItemsControl ItemsSource="{Binding RecentActivity}" MaxHeight="240">` opening tag. Before its `<ItemsControl.Template>` block, add an `<ItemsControl.ItemContainerStyle>`:

```xml
<ItemsControl.ItemContainerStyle>
    <Style TargetType="ContentPresenter">
        <Setter Property="Opacity" Value="0"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <TranslateTransform Y="-8"/>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                         From="0" To="1"
                                         Duration="{StaticResource Duration.Standard}"
                                         EasingFunction="{StaticResource Easing.Standard}"/>
                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                         From="-8" To="0"
                                         Duration="{StaticResource Duration.Slow}"
                                         EasingFunction="{StaticResource Easing.Standard}"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>
</ItemsControl.ItemContainerStyle>
```

- [ ] **Step 2: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```
git add src/GraceKeeper.UI/Views/DashboardView.xaml
git commit -m "ui(dashboard): slide-in storyboard for activity log entries"
```

---

## Task 5: Status dot pulse

**Files:**
- Modify: `src/GraceKeeper.UI/Views/DashboardView.xaml`

- [ ] **Step 1: Replace the static Ellipse with a styled Ellipse**

Find:
```xml
<Ellipse Width="8" Height="8"
         Fill="{Binding StatusText, Converter={StaticResource HealthToColor}}"
         VerticalAlignment="Center" Margin="0,0,6,0"/>
```

Replace with:
```xml
<Ellipse Width="8" Height="8"
         Fill="{Binding StatusText, Converter={StaticResource HealthToColor}}"
         VerticalAlignment="Center" Margin="0,0,6,0">
    <Ellipse.Style>
        <Style TargetType="Ellipse">
            <Setter Property="Opacity" Value="1.0"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding StatusText}" Value="Healthy">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard Name="PulseStory">
                            <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="1.0" To="0.7"
                                                 Duration="{StaticResource Duration.Pulse}"
                                                 EasingFunction="{StaticResource Easing.Standard}"/>
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="PulseStory"/>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Ellipse.Style>
</Ellipse>
```

- [ ] **Step 2: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```
git add src/GraceKeeper.UI/Views/DashboardView.xaml
git commit -m "ui(dashboard): pulse status dot when Healthy"
```

---

## Task 6: CleanerCompletionWatcher in Core (with tests)

**Files:**
- Create: `src/GraceKeeper.Core/CleanerCompletionWatcher.cs`
- Create: `src/GraceKeeper.Core.Tests/CleanerCompletionWatcherTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/GraceKeeper.Core.Tests/CleanerCompletionWatcherTests.cs`:

```csharp
using System;
using System.IO;
using System.Threading;
using System.Threading.Tasks;
using GraceKeeper.Core;
using Xunit;

namespace GraceKeeper.Core.Tests;

public class CleanerCompletionWatcherTests : IDisposable
{
    private readonly string _tmpDir;
    private readonly string _logPath;

    public CleanerCompletionWatcherTests()
    {
        _tmpDir = Path.Combine(Path.GetTempPath(), "gk-cleanwatch-" + Guid.NewGuid().ToString("N"));
        Directory.CreateDirectory(_tmpDir);
        _logPath = Path.Combine(_tmpDir, "cleaner.log");
        File.WriteAllText(_logPath, "");
    }

    public void Dispose()
    {
        try { Directory.Delete(_tmpDir, true); } catch { /* best effort */ }
    }

    [Fact]
    public async Task ReturnsCompleted_WhenNewCleanEntryAppears()
    {
        // existing log starts empty, offset 0
        var watcher = new CleanerCompletionWatcher(_logPath, pollInterval: TimeSpan.FromMilliseconds(50));
        var task = watcher.WaitAsync(startOffset: 0, timeout: TimeSpan.FromSeconds(5), CancellationToken.None);

        // simulate the cleaner finishing
        await Task.Delay(100);
        File.AppendAllText(_logPath, "2026-05-17T12:00:00Z clean deleted=3\n");

        var result = await task;
        Assert.Equal(CleanerCompletionResult.Completed, result);
    }

    [Fact]
    public async Task ReturnsTimedOut_WhenNoNewEntryWithinTimeout()
    {
        var watcher = new CleanerCompletionWatcher(_logPath, pollInterval: TimeSpan.FromMilliseconds(50));
        var result = await watcher.WaitAsync(startOffset: 0, timeout: TimeSpan.FromMilliseconds(200), CancellationToken.None);
        Assert.Equal(CleanerCompletionResult.TimedOut, result);
    }

    [Fact]
    public async Task IgnoresEntriesBeforeStartOffset()
    {
        // seed an existing clean entry
        File.WriteAllText(_logPath, "2026-05-17T11:00:00Z clean deleted=1\n");
        var startOffset = new FileInfo(_logPath).Length;

        var watcher = new CleanerCompletionWatcher(_logPath, pollInterval: TimeSpan.FromMilliseconds(50));
        var task = watcher.WaitAsync(startOffset, timeout: TimeSpan.FromMilliseconds(400), CancellationToken.None);

        // no new entry appended; should time out
        var result = await task;
        Assert.Equal(CleanerCompletionResult.TimedOut, result);
    }
}
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj --filter "FullyQualifiedName~CleanerCompletionWatcherTests"`
Expected: Compile error — `CleanerCompletionWatcher` / `CleanerCompletionResult` not defined.

- [ ] **Step 3: Implement the watcher**

Create `src/GraceKeeper.Core/CleanerCompletionWatcher.cs`:

```csharp
using System;
using System.IO;
using System.Threading;
using System.Threading.Tasks;

namespace GraceKeeper.Core;

public enum CleanerCompletionResult
{
    Completed,
    TimedOut,
}

public sealed class CleanerCompletionWatcher
{
    private readonly string _logPath;
    private readonly TimeSpan _pollInterval;

    public CleanerCompletionWatcher(string logPath, TimeSpan? pollInterval = null)
    {
        _logPath = logPath;
        _pollInterval = pollInterval ?? TimeSpan.FromMilliseconds(500);
    }

    public async Task<CleanerCompletionResult> WaitAsync(long startOffset, TimeSpan timeout, CancellationToken ct)
    {
        var deadline = DateTime.UtcNow + timeout;
        while (DateTime.UtcNow < deadline)
        {
            ct.ThrowIfCancellationRequested();
            if (HasNewEntryPast(startOffset)) return CleanerCompletionResult.Completed;
            try { await Task.Delay(_pollInterval, ct); }
            catch (OperationCanceledException) { throw; }
        }
        return HasNewEntryPast(startOffset) ? CleanerCompletionResult.Completed : CleanerCompletionResult.TimedOut;
    }

    private bool HasNewEntryPast(long startOffset)
    {
        if (!File.Exists(_logPath)) return false;
        var len = new FileInfo(_logPath).Length;
        return len > startOffset;
    }
}
```

- [ ] **Step 4: Run tests, verify they pass**

Run: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj --filter "FullyQualifiedName~CleanerCompletionWatcherTests"`
Expected: 3 passed.

- [ ] **Step 5: Run full Core test suite to verify nothing else broke**

Run: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj -c Release`
Expected: All tests pass.

- [ ] **Step 6: Commit**

```
git add src/GraceKeeper.Core/CleanerCompletionWatcher.cs src/GraceKeeper.Core.Tests/CleanerCompletionWatcherTests.cs
git commit -m "core: CleanerCompletionWatcher polls cleaner log for new entries with timeout"
```

---

## Task 7: DashboardViewModel CleanerState wiring

**Files:**
- Modify: `src/GraceKeeper.UI/ViewModels/DashboardViewModel.cs`

- [ ] **Step 1: Add the CleanerButtonState enum**

At the bottom of `src/GraceKeeper.UI/ViewModels/DashboardViewModel.cs` (after the `DashboardViewModel` class closing brace, still inside the namespace), add:

```csharp
public enum CleanerButtonState
{
    Idle,
    Running,
    Done,
}
```

- [ ] **Step 2: Add the CleanerState property**

Inside the `DashboardViewModel` class, add a backing field and property near the other state fields (near line 27):

```csharp
private CleanerButtonState _cleanerState = CleanerButtonState.Idle;

public CleanerButtonState CleanerState
{
    get => _cleanerState;
    private set => Set(ref _cleanerState, value);
}
```

- [ ] **Step 3: Rework RunCleanerNowAsync**

Replace the existing one-liner `RunCleanerNowAsync` method:
```csharp
public async Task RunCleanerNowAsync() => await _scheduler.RunNowAsync();
```

with:

```csharp
public async Task RunCleanerNowAsync()
{
    if (CleanerState != CleanerButtonState.Idle) return;

    var cfg = _configStore.Load();
    var startOffset = cfg.LogOffsets.CleanerLogOffset;

    CleanerState = CleanerButtonState.Running;
    try
    {
        await _scheduler.RunNowAsync();

        var watcher = new CleanerCompletionWatcher(PathResolver.CleanerLogPath);
        var result = await watcher.WaitAsync(startOffset, TimeSpan.FromSeconds(60), default);

        if (result == CleanerCompletionResult.Completed)
        {
            CleanerState = CleanerButtonState.Done;
            await Task.Delay(1200);
            CleanerState = CleanerButtonState.Idle;
        }
        else
        {
            // timed out — fall back to Idle (don't claim success)
            CleanerState = CleanerButtonState.Idle;
        }
    }
    catch
    {
        CleanerState = CleanerButtonState.Idle;
        throw;
    }
}
```

- [ ] **Step 4: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 5: Commit**

```
git add src/GraceKeeper.UI/ViewModels/DashboardViewModel.cs
git commit -m "ui(dashboard): track CleanerButtonState through RunCleanerNowAsync"
```

---

## Task 8: RunCleanerButton control

**Files:**
- Create: `src/GraceKeeper.UI/Controls/RunCleanerButton.cs`
- Create: `src/GraceKeeper.UI/Controls/RunCleanerButton.xaml`
- Modify: `src/GraceKeeper.UI/App.xaml`

- [ ] **Step 1: Create the control class**

Create `src/GraceKeeper.UI/Controls/RunCleanerButton.cs`:

```csharp
using System.Windows;
using System.Windows.Controls.Primitives;
using GraceKeeper.UI.ViewModels;

namespace GraceKeeper.UI.Controls;

public class RunCleanerButton : ButtonBase
{
    static RunCleanerButton()
    {
        DefaultStyleKeyProperty.OverrideMetadata(
            typeof(RunCleanerButton),
            new FrameworkPropertyMetadata(typeof(RunCleanerButton)));
    }

    public static readonly DependencyProperty StateProperty =
        DependencyProperty.Register(
            nameof(State), typeof(CleanerButtonState), typeof(RunCleanerButton),
            new PropertyMetadata(CleanerButtonState.Idle, OnStateChanged));

    public CleanerButtonState State
    {
        get => (CleanerButtonState)GetValue(StateProperty);
        set => SetValue(StateProperty, value);
    }

    public override void OnApplyTemplate()
    {
        base.OnApplyTemplate();
        UpdateVisualState(useTransitions: false);
    }

    private static void OnStateChanged(DependencyObject d, DependencyPropertyChangedEventArgs e)
    {
        ((RunCleanerButton)d).UpdateVisualState(useTransitions: true);
    }

    private void UpdateVisualState(bool useTransitions)
    {
        var stateName = State switch
        {
            CleanerButtonState.Running => "Running",
            CleanerButtonState.Done => "Done",
            _ => "Idle",
        };
        VisualStateManager.GoToState(this, stateName, useTransitions);
    }
}
```

- [ ] **Step 2: Create the default template**

Create `src/GraceKeeper.UI/Controls/RunCleanerButton.xaml`. The Done-state tint is implemented as a second Border layered above the Brand.Red one, opacity-faded in and out — this avoids the "cannot animate frozen brush" trap that hits us if we try to `ColorAnimation` a DynamicResource brush, while keeping the base Brand.Red theme-aware:

```xml
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:ctl="clr-namespace:GraceKeeper.UI.Controls">
    <Style TargetType="{x:Type ctl:RunCleanerButton}">
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ctl:RunCleanerButton}">
                    <Grid>
                        <Border x:Name="RedBg"
                                Background="{DynamicResource Brand.Red}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"/>
                        <Border x:Name="GreenBg"
                                Background="{DynamicResource Semantic.Good}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}"
                                Opacity="0"/>
                        <Grid Margin="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <!-- Spinner -->
                                <Grid x:Name="Spinner" Width="14" Height="14" Margin="0,0,8,0" Opacity="0">
                                    <Grid.RenderTransform>
                                        <RotateTransform x:Name="SpinnerRotate" Angle="0" CenterX="7" CenterY="7"/>
                                    </Grid.RenderTransform>
                                    <Ellipse Stroke="White" StrokeThickness="2" Opacity="0.25"/>
                                    <Path Stroke="White" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                          Data="M 7 0 A 7 7 0 0 1 14 7"/>
                                </Grid>
                                <!-- Done check -->
                                <Path x:Name="Check" Stroke="White" StrokeThickness="2" Margin="0,0,8,0"
                                      StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                      Data="M 0 6 L 4 10 L 11 2" Opacity="0"/>
                                <!-- Label -->
                                <TextBlock x:Name="Label" Text="Run cleaner now" Foreground="{TemplateBinding Foreground}" FontWeight="{TemplateBinding FontWeight}"/>
                            </StackPanel>
                        </Grid>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup Name="CleanerStates">
                                <VisualState Name="Idle">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Spinner" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Standard}"/>
                                        <DoubleAnimation Storyboard.TargetName="Check" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Standard}"/>
                                        <DoubleAnimation Storyboard.TargetName="GreenBg" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Standard}"/>
                                        <StringAnimationUsingKeyFrames Storyboard.TargetName="Label" Storyboard.TargetProperty="Text" Duration="0:0:0">
                                            <DiscreteStringKeyFrame KeyTime="0:0:0" Value="Run cleaner now"/>
                                        </StringAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState Name="Running">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Spinner" Storyboard.TargetProperty="Opacity" To="1" Duration="{StaticResource Duration.Standard}"/>
                                        <DoubleAnimation Storyboard.TargetName="Check" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Quick}"/>
                                        <DoubleAnimation Storyboard.TargetName="GreenBg" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Standard}"/>
                                        <DoubleAnimation Storyboard.TargetName="SpinnerRotate" Storyboard.TargetProperty="Angle"
                                                         From="0" To="360" Duration="0:0:0.9" RepeatBehavior="Forever"/>
                                        <StringAnimationUsingKeyFrames Storyboard.TargetName="Label" Storyboard.TargetProperty="Text" Duration="0:0:0">
                                            <DiscreteStringKeyFrame KeyTime="0:0:0" Value="Cleaning…"/>
                                        </StringAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState Name="Done">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="Spinner" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Quick}"/>
                                        <DoubleAnimation Storyboard.TargetName="Check" Storyboard.TargetProperty="Opacity" To="1" Duration="{StaticResource Duration.Standard}"/>
                                        <DoubleAnimation Storyboard.TargetName="GreenBg" Storyboard.TargetProperty="Opacity" To="1" Duration="{StaticResource Duration.Standard}"/>
                                        <StringAnimationUsingKeyFrames Storyboard.TargetName="Label" Storyboard.TargetProperty="Text" Duration="0:0:0">
                                            <DiscreteStringKeyFrame KeyTime="0:0:0" Value="Done"/>
                                        </StringAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
```

- [ ] **Step 3: Merge the new resource dictionary into App.xaml**

Edit `src/GraceKeeper.UI/App.xaml`. Add to the merged dictionaries (alongside AnimatedCounter.xaml):

```xml
<ResourceDictionary Source="Controls/RunCleanerButton.xaml" />
```

- [ ] **Step 4: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 5: Commit**

```
git add src/GraceKeeper.UI/Controls/RunCleanerButton.cs src/GraceKeeper.UI/Controls/RunCleanerButton.xaml src/GraceKeeper.UI/App.xaml
git commit -m "ui: add RunCleanerButton control with Idle/Running/Done visual states"
```

---

## Task 9: Wire RunCleanerButton into DashboardView

**Files:**
- Modify: `src/GraceKeeper.UI/Views/DashboardView.xaml`
- Modify: `src/GraceKeeper.UI/Views/DashboardView.xaml.cs`

- [ ] **Step 1: Replace the existing Run cleaner button**

In `src/GraceKeeper.UI/Views/DashboardView.xaml`, find:

```xml
<Button Content="Run cleaner now" Background="{DynamicResource Brand.Red}" Foreground="White"
        Padding="14,8" BorderThickness="0" Click="RunCleaner_Click"/>
```

Replace with:

```xml
<ctl:RunCleanerButton State="{Binding CleanerState}" Click="RunCleaner_Click"/>
```

- [ ] **Step 2: Confirm the click handler still works**

`DashboardView.xaml.cs` already calls `await Vm.RunCleanerNowAsync()` from `RunCleaner_Click`. No changes needed.

- [ ] **Step 3: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 4: Commit**

```
git add src/GraceKeeper.UI/Views/DashboardView.xaml
git commit -m "ui(dashboard): use RunCleanerButton in action row"
```

---

## Task 10: PhaseProgressTracker in Core (with tests)

**Files:**
- Create: `src/GraceKeeper.Core/PhaseProgressTracker.cs`
- Create: `src/GraceKeeper.Core.Tests/PhaseProgressTrackerTests.cs`

- [ ] **Step 1: Write the failing test**

Create `src/GraceKeeper.Core.Tests/PhaseProgressTrackerTests.cs`:

```csharp
using System.Linq;
using GraceKeeper.Core;
using Xunit;

namespace GraceKeeper.Core.Tests;

public class PhaseProgressTrackerTests
{
    [Fact]
    public void AllPhasesStartPending()
    {
        var t = new PhaseProgressTracker(new[] { "A", "B", "C" });
        Assert.All(t.Phases, p => Assert.Equal(PhaseState.Pending, p.State));
    }

    [Fact]
    public void BeginActivatesFirstPhase()
    {
        var t = new PhaseProgressTracker(new[] { "A", "B", "C" });
        t.Begin();
        Assert.Equal(PhaseState.Active, t.Phases[0].State);
        Assert.Equal(PhaseState.Pending, t.Phases[1].State);
        Assert.Equal(PhaseState.Pending, t.Phases[2].State);
    }

    [Fact]
    public void AdvanceMovesToNextPhase()
    {
        var t = new PhaseProgressTracker(new[] { "A", "B", "C" });
        t.Begin();
        t.Advance();
        Assert.Equal(PhaseState.Done, t.Phases[0].State);
        Assert.Equal(PhaseState.Active, t.Phases[1].State);
        Assert.Equal(PhaseState.Pending, t.Phases[2].State);
    }

    [Fact]
    public void CompleteMarksFinalAsDone()
    {
        var t = new PhaseProgressTracker(new[] { "A", "B", "C" });
        t.Begin();
        t.Advance();
        t.Advance();
        t.Complete();
        Assert.All(t.Phases, p => Assert.Equal(PhaseState.Done, p.State));
    }

    [Fact]
    public void AdvancePastEndIsSafeNoOp()
    {
        var t = new PhaseProgressTracker(new[] { "A" });
        t.Begin();
        t.Advance();   // completes A
        t.Advance();   // no-op
        Assert.Equal(PhaseState.Done, t.Phases[0].State);
    }

    [Fact]
    public void PhasesAreObservableForUiBinding()
    {
        var t = new PhaseProgressTracker(new[] { "A", "B" });
        Assert.NotNull(t.Phases as System.Collections.Specialized.INotifyCollectionChanged);
    }

    [Fact]
    public void PhaseRowRaisesPropertyChangedOnStateChange()
    {
        var t = new PhaseProgressTracker(new[] { "A" });
        var row = t.Phases[0];
        var changes = 0;
        row.PropertyChanged += (_, _) => changes++;
        t.Begin();
        Assert.True(changes > 0);
    }
}
```

- [ ] **Step 2: Run test to verify failure**

Run: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj --filter "FullyQualifiedName~PhaseProgressTrackerTests"`
Expected: Compile error — `PhaseProgressTracker` / `PhaseState` / `PhaseRow` not defined.

- [ ] **Step 3: Implement**

Create `src/GraceKeeper.Core/PhaseProgressTracker.cs`:

```csharp
using System.Collections.Generic;
using System.Collections.ObjectModel;
using System.ComponentModel;
using System.Runtime.CompilerServices;

namespace GraceKeeper.Core;

public enum PhaseState
{
    Pending,
    Active,
    Done,
}

public sealed class PhaseRow : INotifyPropertyChanged
{
    private string _title;
    private PhaseState _state;

    public PhaseRow(string title)
    {
        _title = title;
    }

    public string Title
    {
        get => _title;
        set { if (_title == value) return; _title = value; OnPropertyChanged(); }
    }

    public PhaseState State
    {
        get => _state;
        set { if (_state == value) return; _state = value; OnPropertyChanged(); }
    }

    public event PropertyChangedEventHandler? PropertyChanged;
    private void OnPropertyChanged([CallerMemberName] string? name = null)
        => PropertyChanged?.Invoke(this, new PropertyChangedEventArgs(name));
}

public sealed class PhaseProgressTracker
{
    private int _activeIndex = -1;

    public PhaseProgressTracker(IEnumerable<string> titles)
    {
        Phases = new ObservableCollection<PhaseRow>();
        foreach (var t in titles) Phases.Add(new PhaseRow(t));
    }

    public ObservableCollection<PhaseRow> Phases { get; }

    public void Begin()
    {
        if (Phases.Count == 0) return;
        _activeIndex = 0;
        Phases[0].State = PhaseState.Active;
    }

    public void Advance()
    {
        if (_activeIndex < 0 || _activeIndex >= Phases.Count) return;
        Phases[_activeIndex].State = PhaseState.Done;
        _activeIndex++;
        if (_activeIndex < Phases.Count)
            Phases[_activeIndex].State = PhaseState.Active;
    }

    public void Complete()
    {
        foreach (var p in Phases) p.State = PhaseState.Done;
        _activeIndex = Phases.Count;
    }
}
```

- [ ] **Step 4: Run tests**

Run: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj --filter "FullyQualifiedName~PhaseProgressTrackerTests"`
Expected: 7 passed.

- [ ] **Step 5: Run full Core test suite**

Run: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj -c Release`
Expected: All tests pass.

- [ ] **Step 6: Commit**

```
git add src/GraceKeeper.Core/PhaseProgressTracker.cs src/GraceKeeper.Core.Tests/PhaseProgressTrackerTests.cs
git commit -m "core: PhaseProgressTracker state machine for installer phase rows"
```

---

## Task 11: PhaseList control (bootstrapper)

**Files:**
- Create: `src/GraceKeeper.Bootstrapper/Controls/PhaseList.xaml`
- Create: `src/GraceKeeper.Bootstrapper/Controls/PhaseList.xaml.cs`

- [ ] **Step 1: Create the UserControl XAML**

Create `src/GraceKeeper.Bootstrapper/Controls/PhaseList.xaml`. Text-color changes use discrete `Setter`s (not `ColorAnimation`) — fast color flicks read fine without a tween, and this sidesteps the frozen-brush trap that bites theme-resource brushes:

```xml
<UserControl x:Class="GraceKeeper.Bootstrapper.Controls.PhaseList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:core="clr-namespace:GraceKeeper.Core;assembly=GraceKeeper.Core">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type core:PhaseRow}">
            <Grid Margin="0,4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="22"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0" Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <!-- Pending dot -->
                    <Ellipse x:Name="Dot" Width="6" Height="6" Fill="{StaticResource Border}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <!-- Active spinner -->
                    <Grid x:Name="Spinner" Opacity="0">
                        <Grid.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" Angle="0" CenterX="8" CenterY="8"/>
                        </Grid.RenderTransform>
                        <Ellipse Stroke="{StaticResource Accent}" StrokeThickness="2" Opacity="0.25"/>
                        <Path Stroke="{StaticResource Accent}" StrokeThickness="2" StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                              Data="M 8 0 A 8 8 0 0 1 16 8"/>
                    </Grid>
                    <!-- Done check -->
                    <Path x:Name="Check" Stroke="{StaticResource Accent}" StrokeThickness="2" Opacity="0"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                          Data="M 1 8 L 6 13 L 14 3"/>
                </Grid>
                <TextBlock x:Name="TitleText" Grid.Column="1" Text="{Binding Title}" VerticalAlignment="Center"
                           FontSize="12" Foreground="{StaticResource MutedText}"/>
                <Grid.Triggers>
                    <DataTrigger Binding="{Binding State}" Value="Active">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="Dot" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Standard}"/>
                                    <DoubleAnimation Storyboard.TargetName="Spinner" Storyboard.TargetProperty="Opacity" To="1" Duration="{StaticResource Duration.Standard}"/>
                                    <DoubleAnimation Storyboard.TargetName="Check" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Quick}"/>
                                    <DoubleAnimation Storyboard.TargetName="SpinnerRotate" Storyboard.TargetProperty="Angle"
                                                     From="0" To="360" Duration="0:0:0.9" RepeatBehavior="Forever"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                    </DataTrigger>
                    <DataTrigger Binding="{Binding State}" Value="Done">
                        <DataTrigger.EnterActions>
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="Spinner" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Quick}"/>
                                    <DoubleAnimation Storyboard.TargetName="Dot" Storyboard.TargetProperty="Opacity" To="0" Duration="{StaticResource Duration.Quick}"/>
                                    <DoubleAnimation Storyboard.TargetName="Check" Storyboard.TargetProperty="Opacity" To="1" Duration="{StaticResource Duration.Standard}"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </DataTrigger.EnterActions>
                    </DataTrigger>
                </Grid.Triggers>
            </Grid>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding State}" Value="Active">
                    <Setter TargetName="TitleText" Property="Foreground" Value="{StaticResource Text}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding State}" Value="Done">
                    <Setter TargetName="TitleText" Property="Foreground" Value="{StaticResource MutedText}"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>
    <ItemsControl ItemsSource="{Binding Phases, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
</UserControl>
```

- [ ] **Step 2: Create the code-behind**

Create `src/GraceKeeper.Bootstrapper/Controls/PhaseList.xaml.cs`:

```csharp
using System.Collections.ObjectModel;
using System.Windows;
using System.Windows.Controls;
using GraceKeeper.Core;

namespace GraceKeeper.Bootstrapper.Controls;

public partial class PhaseList : UserControl
{
    public static readonly DependencyProperty PhasesProperty =
        DependencyProperty.Register(
            nameof(Phases),
            typeof(ObservableCollection<PhaseRow>),
            typeof(PhaseList),
            new PropertyMetadata(null));

    public ObservableCollection<PhaseRow> Phases
    {
        get => (ObservableCollection<PhaseRow>)GetValue(PhasesProperty);
        set => SetValue(PhasesProperty, value);
    }

    public PhaseList()
    {
        InitializeComponent();
    }
}
```

- [ ] **Step 3: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds. WPF generates the partial class from the XAML.

- [ ] **Step 4: Commit**

```
git add src/GraceKeeper.Bootstrapper/Controls/PhaseList.xaml src/GraceKeeper.Bootstrapper/Controls/PhaseList.xaml.cs
git commit -m "installer: add PhaseList UserControl with dot/spinner/check states"
```

---

## Task 12: MainViewModel — SmoothedPercent, IsRunning, Phases

**Files:**
- Modify: `src/GraceKeeper.Bootstrapper/ViewModels/MainViewModel.cs`

- [ ] **Step 1: Add the new fields and properties**

Open `src/GraceKeeper.Bootstrapper/ViewModels/MainViewModel.cs`. Add `using GraceKeeper.Core;` at the top.

Inside the class, near the other backing fields, add:

```csharp
private double _smoothedPercent;
private bool _isRunning;
private readonly PhaseProgressTracker _tracker;
```

In the constructor (after the `_ba.ApplyComplete += ...` line), add:

```csharp
_tracker = new PhaseProgressTracker(new[] { "Preparing", "Installing", "Finishing" });
```

Add properties near the existing public properties:

```csharp
public double SmoothedPercent { get => _smoothedPercent; private set { _smoothedPercent = value; OnPropertyChanged(); } }
public bool IsRunning { get => _isRunning; private set { _isRunning = value; OnPropertyChanged(); } }
public System.Collections.ObjectModel.ObservableCollection<PhaseRow> Phases => _tracker.Phases;
```

- [ ] **Step 2: Replace the Percent setter to animate SmoothedPercent**

Current:
```csharp
public int Percent { get => _percent; set { _percent = value; OnPropertyChanged(); } }
```

Replace with:
```csharp
public int Percent
{
    get => _percent;
    set
    {
        _percent = value;
        OnPropertyChanged();
        AnimateSmoothedPercent(value);
    }
}

private void AnimateSmoothedPercent(int target)
{
    var startValue = _smoothedPercent;        // capture once — _smoothedPercent mutates each tick
    var durationMs = 1200.0;
    var easing = new System.Windows.Media.Animation.CubicEase
    {
        EasingMode = System.Windows.Media.Animation.EasingMode.EaseOut,
    };
    var anim = new System.Windows.Media.Animation.DoubleAnimation
    {
        From = startValue,
        To = target,
        Duration = new System.Windows.Duration(System.TimeSpan.FromMilliseconds(durationMs)),
        EasingFunction = easing,
        FillBehavior = System.Windows.Media.Animation.FillBehavior.HoldEnd,
    };
    var clock = anim.CreateClock();
    clock.CurrentTimeInvalidated += (_, _) =>
    {
        if (clock.CurrentTime is System.TimeSpan ct)
        {
            var fraction = ct.TotalMilliseconds / durationMs;
            if (fraction > 1) fraction = 1;
            var eased = easing.Ease(fraction);
            SmoothedPercent = startValue + (target - startValue) * eased;
        }
    };
    clock.Controller?.Begin();
}
```

(Note: the easier path is `BeginAnimation` on a DP, but `SmoothedPercent` is a normal CLR property. The clock-based approach above gives us the per-frame tick without making SmoothedPercent a DP. Alternative: convert `SmoothedPercent` to a DP on a small helper DependencyObject and bind through it. Use the clock approach as written.)

- [ ] **Step 3: Hook phase advancement**

Modify `DoInstall` to start the tracker:

```csharp
private void DoInstall()
{
    CurrentPage = new ProgressPage { DataContext = this };
    StatusText = "Preparing…";
    IsRunning = true;
    _tracker.Begin();
    _engine.Plan(LaunchAction.Install);
}
```

Modify `DoUninstall` likewise:

```csharp
private void DoUninstall()
{
    CurrentPage = new ProgressPage { DataContext = this };
    StatusText = "Removing GraceKeeper…";
    IsRunning = true;
    _tracker.Begin();
    _engine.Plan(LaunchAction.Uninstall);
}
```

In `OnPlanComplete` (inside `Application.Current.Dispatcher.Invoke`, on success branch — before the `_engine.Apply(hwnd)` call), add:

```csharp
_tracker.Advance();  // Preparing -> Done, Installing -> Active
```

In `OnProgress`, after setting `Percent = p;`, check for 100 once:

```csharp
private void OnProgress(object? sender, ProgressEventArgs e)
{
    var p = e.OverallPercentage;
    Application.Current.Dispatcher.Invoke(() =>
    {
        Percent = p;
        if (p >= 100 && _tracker.Phases[1].State == PhaseState.Active)
        {
            _tracker.Advance();  // Installing -> Done, Finishing -> Active
        }
    });
}
```

In `OnApplyComplete`, on the success branch (before assigning `CurrentPage = ...`), add a brief delay then mark complete:

```csharp
_tracker.Complete();
IsRunning = false;
```

Place those two lines right after the `if (e.Status >= 0)` opens, before the existing branching on `_command.Action`. The existing FinishPage / UninstallFinishPage assignment continues to happen on the same tick — that's fine; the 300ms pause described in the spec for UX polish can be deferred to the page-transition storyboard (Task 15).

- [ ] **Step 4: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 5: Run the Core test suite to confirm nothing in Core regressed**

Run: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj -c Release`
Expected: All tests pass.

- [ ] **Step 6: Commit**

```
git add src/GraceKeeper.Bootstrapper/ViewModels/MainViewModel.cs
git commit -m "installer(vm): smoothed progress + phase tracker wired to Burn lifecycle"
```

---

## Task 13: ProgressPage — smoothed bar, shimmer, PhaseList

**Files:**
- Modify: `src/GraceKeeper.Bootstrapper/Views/ProgressPage.xaml`

- [ ] **Step 1: Add the controls namespace**

In the `<UserControl ...>` root, add `xmlns:ctl="clr-namespace:GraceKeeper.Bootstrapper.Controls"`.

- [ ] **Step 2: Replace the existing ProgressBar with a smoothed bar + shimmer**

Find:
```xml
<ProgressBar Margin="0,22,0,0" Height="4" Minimum="0" Maximum="100" Value="{Binding Percent}"
             Background="#FF2A2A2A" Foreground="{StaticResource Accent}" BorderThickness="0"/>
```

Replace with:
```xml
<Grid Margin="0,22,0,0" Height="4">
    <ProgressBar Minimum="0" Maximum="100" Value="{Binding SmoothedPercent}"
                 Background="#FF2A2A2A" Foreground="{StaticResource Accent}" BorderThickness="0"/>
    <Rectangle IsHitTestVisible="False">
        <Rectangle.Style>
            <Style TargetType="Rectangle">
                <Setter Property="Visibility" Value="Collapsed"/>
                <Style.Triggers>
                    <DataTrigger Binding="{Binding IsRunning}" Value="True">
                        <Setter Property="Visibility" Value="Visible"/>
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Rectangle.Style>
        <Rectangle.Fill>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                <GradientStop x:Name="ShimmerStart" Color="#00FFFFFF" Offset="-0.3"/>
                <GradientStop x:Name="ShimmerMid" Color="#2EFFFFFF" Offset="0.0"/>
                <GradientStop x:Name="ShimmerEnd" Color="#00FFFFFF" Offset="0.3"/>
            </LinearGradientBrush>
        </Rectangle.Fill>
        <Rectangle.Triggers>
            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                <BeginStoryboard>
                    <Storyboard RepeatBehavior="Forever">
                        <DoubleAnimation Storyboard.TargetName="ShimmerStart" Storyboard.TargetProperty="Offset" From="-0.3" To="1.0" Duration="0:0:1.8"/>
                        <DoubleAnimation Storyboard.TargetName="ShimmerMid" Storyboard.TargetProperty="Offset" From="0.0" To="1.3" Duration="0:0:1.8"/>
                        <DoubleAnimation Storyboard.TargetName="ShimmerEnd" Storyboard.TargetProperty="Offset" From="0.3" To="1.6" Duration="0:0:1.8"/>
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Rectangle.Triggers>
    </Rectangle>
</Grid>
```

- [ ] **Step 3: Mount the PhaseList below the bar**

Inside the `<StackPanel Grid.Row="2" ...>` (the one that contains "Installing…" and StatusText and the bar Grid), after the bar Grid, add:

```xml
<ctl:PhaseList Phases="{Binding Phases}" Margin="0,20,0,0"/>
```

The existing `<TextBlock Text="{Binding StatusText}" ... />` line stays — it's the verbatim Burn MSI message, useful as fine-print under the phase list. Move it to render after the PhaseList:

Reorder the StackPanel children so they read:
1. `<TextBlock Text="Installing…" .../>` (header)
2. `<Grid Margin="0,22,0,0" Height="4"> ... </Grid>` (the bar + shimmer)
3. `<ctl:PhaseList Phases="{Binding Phases}" Margin="0,20,0,0"/>`
4. `<TextBlock Text="{Binding StatusText}" Margin="0,16,0,0" FontSize="11" Foreground="{StaticResource MutedText}" FontFamily="Consolas"/>`

- [ ] **Step 4: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 5: Commit**

```
git add src/GraceKeeper.Bootstrapper/Views/ProgressPage.xaml
git commit -m "installer(progress): smoothed bar + shimmer overlay + phase list"
```

---

## Task 14: WelcomePage hero word cascade

**Files:**
- Modify: `src/GraceKeeper.Bootstrapper/Views/WelcomePage.xaml`

- [ ] **Step 1: Replace the hero TextBlock with a per-word StackPanel**

Find:
```xml
<TextBlock Grid.Row="0" FontSize="26" FontWeight="Light" Foreground="White" TextWrapping="Wrap">
    <Run Text="Keep the grace period "/><Run Text="alive" Foreground="{StaticResource Accent}"/><Run Text="."/>
</TextBlock>
```

Replace with a horizontal StackPanel of word TextBlocks, each with its own opacity and transform:

```xml
<StackPanel Grid.Row="0" Orientation="Horizontal">
    <StackPanel.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Opacity" Value="0"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <TranslateTransform Y="8"/>
                </Setter.Value>
            </Setter>
        </Style>
    </StackPanel.Resources>
    <StackPanel.Triggers>
        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <!-- Word 1: Keep -->
                    <DoubleAnimation Storyboard.TargetName="W1" Storyboard.TargetProperty="Opacity" From="0" To="1" BeginTime="0:0:0" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                    <DoubleAnimation Storyboard.TargetName="W1" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="8" To="0" BeginTime="0:0:0" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                    <!-- Word 2: the -->
                    <DoubleAnimation Storyboard.TargetName="W2" Storyboard.TargetProperty="Opacity" From="0" To="1" BeginTime="0:0:0.06" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                    <DoubleAnimation Storyboard.TargetName="W2" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="8" To="0" BeginTime="0:0:0.06" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                    <!-- Word 3: grace -->
                    <DoubleAnimation Storyboard.TargetName="W3" Storyboard.TargetProperty="Opacity" From="0" To="1" BeginTime="0:0:0.12" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                    <DoubleAnimation Storyboard.TargetName="W3" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="8" To="0" BeginTime="0:0:0.12" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                    <!-- Word 4: period -->
                    <DoubleAnimation Storyboard.TargetName="W4" Storyboard.TargetProperty="Opacity" From="0" To="1" BeginTime="0:0:0.18" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                    <DoubleAnimation Storyboard.TargetName="W4" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="8" To="0" BeginTime="0:0:0.18" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                    <!-- Word 5: alive -->
                    <DoubleAnimation Storyboard.TargetName="W5" Storyboard.TargetProperty="Opacity" From="0" To="1" BeginTime="0:0:0.24" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                    <DoubleAnimation Storyboard.TargetName="W5" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="8" To="0" BeginTime="0:0:0.24" Duration="{StaticResource Duration.Slow}" EasingFunction="{StaticResource Easing.Standard}"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </StackPanel.Triggers>
    <TextBlock x:Name="W1" Text="Keep" Margin="0,0,8,0"/>
    <TextBlock x:Name="W2" Text="the" Margin="0,0,8,0"/>
    <TextBlock x:Name="W3" Text="grace" Margin="0,0,8,0"/>
    <TextBlock x:Name="W4" Text="period" Margin="0,0,8,0"/>
    <TextBlock x:Name="W5" Text="alive." Foreground="{StaticResource Accent}"/>
</StackPanel>
```

- [ ] **Step 2: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```
git add src/GraceKeeper.Bootstrapper/Views/WelcomePage.xaml
git commit -m "installer(welcome): cascade-in animation for the hero line"
```

---

## Task 15: MainWindow page transition

**Files:**
- Modify: `src/GraceKeeper.Bootstrapper/MainWindow.xaml`

- [ ] **Step 1: Replace the plain ContentControl with a templated one that fades+slides in**

Find:
```xml
<Grid>
    <ContentControl Content="{Binding CurrentPage}"/>
</Grid>
```

Replace with:
```xml
<Grid>
    <ContentControl Content="{Binding CurrentPage}">
        <ContentControl.Template>
            <ControlTemplate TargetType="ContentControl">
                <ContentPresenter x:Name="Presenter" Opacity="0">
                    <ContentPresenter.RenderTransform>
                        <TranslateTransform Y="6"/>
                    </ContentPresenter.RenderTransform>
                    <ContentPresenter.Triggers>
                        <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1"
                                                     Duration="{StaticResource Duration.Standard}"
                                                     EasingFunction="{StaticResource Easing.Standard}"/>
                                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                     From="6" To="0"
                                                     Duration="{StaticResource Duration.Standard}"
                                                     EasingFunction="{StaticResource Easing.Standard}"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ContentPresenter.Triggers>
                </ContentPresenter>
            </ControlTemplate>
        </ContentControl.Template>
    </ContentControl>
</Grid>
```

Note: `ContentPresenter.Triggers` only accepts `EventTrigger`s (not `Trigger` / `DataTrigger`). The Loaded event fires every time `CurrentPage` is swapped because a new `ContentPresenter` is realized — verified by Burn's `CurrentPage = new XxxPage { DataContext = this }` pattern.

- [ ] **Step 2: Build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 3: Commit**

```
git add src/GraceKeeper.Bootstrapper/MainWindow.xaml
git commit -m "installer: fade+slide page transitions in MainWindow"
```

---

## Task 16: Update release-checklist.md

**Files:**
- Modify: `docs/release-checklist.md`

- [ ] **Step 1: Add a new "Animation polish (v0.3.0)" section**

In `docs/release-checklist.md`, after the existing "Post-install behavior" section and before "Uninstall behavior", insert:

```markdown
## Animation polish (v0.3.0)

- [ ] **Dashboard counter roll:** On dashboard open, the two large counters (Popups dismissed, .rnl files deleted) animate from 0 to their stored values over ~1.2s with an eased finish. They do not snap.
- [ ] **Dashboard counter live increment:** With a popup dismissed externally, the "Popups dismissed" counter ticks up smoothly (does not flicker or snap).
- [ ] **Activity log slide-in:** New `dismiss` / `clean` rows fade in and slide down 8px on appearance, one after another, not all at once.
- [ ] **Status pill pulse:** When status is Healthy, the green dot's opacity gently pulses (1.0 ↔ 0.7 over ~1.6s, autoreversing). When status changes to Paused, the pulse stops and the dot is static.
- [ ] **Run cleaner button morph:** Click "Run cleaner now"; the button label changes to "Cleaning…" with a rotating spinner. On completion, it briefly flashes green with "Done ✓" before reverting to default (~1.2s).
- [ ] **Run cleaner timeout fallback:** If the cleaner fails to log a completion within 60s, the button reverts to Idle without showing "Done" (we do not lie about success).
- [ ] **Installer progress smoothness:** Run the installer. The progress bar visibly tweens between Burn's percentage updates — it does not teleport from 0 → 50 → 100.
- [ ] **Installer shimmer:** A subtle moving highlight travels left-to-right across the progress bar while installation is running. Shimmer disappears when ApplyComplete fires.
- [ ] **Installer phase list:** Below the bar, three rows appear (Preparing, Installing, Finishing). Each row transitions: pending dot → active spinner → checkmark, in lockstep with Burn lifecycle events.
- [ ] **Welcome cascade:** On installer launch, the hero line "Keep the grace period alive." animates in word-by-word with a small downward fade, 60ms apart.
- [ ] **Page transition:** Switching from Welcome → Progress → Finish, each new page fades in with a 6px upward slide. No flash, no abrupt swap.
```

- [ ] **Step 2: Commit**

```
git add docs/release-checklist.md
git commit -m "docs: release-checklist entries for v0.3.0 animation polish"
```

---

## Final verification

After all tasks complete:

- [ ] **Step 1: Full build**

Run: `dotnet build src/GraceKeeper.sln -c Release`
Expected: Build succeeds.

- [ ] **Step 2: All tests**

Run: `dotnet test src/GraceKeeper.Core.Tests/GraceKeeper.Core.Tests.csproj -c Release`
Expected: All tests pass, including the two new test classes (`PhaseProgressTrackerTests`, `CleanerCompletionWatcherTests`).

- [ ] **Step 3: No hard-coded animation timing**

Run grep to confirm all `Duration="..."` outside theme dictionaries reference `{StaticResource Duration.*}` (a few literal `"0:0:0.9"` values for spinner rotation speed are acceptable — those are visual-rate values, not the standard motion scale).

Run: `grep -rn 'Duration="0:' src/GraceKeeper.UI/Views src/GraceKeeper.Bootstrapper/Views src/GraceKeeper.UI/Controls src/GraceKeeper.Bootstrapper/Controls --include='*.xaml'`
Expected: only the spinner-rotation `"0:0:0.9"` and shimmer `"0:0:1.8"` values appear; no other literal durations.

- [ ] **Step 4: Manual QA per `docs/release-checklist.md` "Animation polish" section**

Walk through each item in person. Anything that doesn't match the spec is a bug.
